# rules/registry.py
import warnings
warnings.warn("'rules.registry' has moved to 'domain.rules.registry'. Please update your imports.", DeprecationWarning, stacklevel=2)
from domain.rules.registry import *